# Run Playwright Tests with Docker

After cloning the project:

1. Build the Docker image:
```bash
docker build -t learnqa-playwright-docker .
2. docker run --rm learnqa-playwright-docker

git pull
docker build -t learnqa-playwright-docker .
docker run --rm learnqa-playwright-docker